import 'dart:ui';

class LightAppColors {
  static const primaryBackground = Color(0xffedeef0);
  static const primaryText = Color(0xff000000);
  static const secondaryBackground = Color(0xffffffff);
}
